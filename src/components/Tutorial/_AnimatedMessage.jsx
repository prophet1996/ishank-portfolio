import React, { Fragment } from "react";
import { WELCOME_MESSAGE } from "../../constants";

const AnimatedMessage = props => {
  return <Fragment>{WELCOME_MESSAGE}</Fragment>;
};
export default AnimatedMessage;
